run-name: Build Container Images by @${{ github.actor }}

on:
  workflow_dispatch:
    inputs:
      image:
        description: "Select Docker Image"
        required: true
        default: "postgres"
        type: choice
        options:
          - "postgres"
          - "postgres-cnpg"
          - "redis"
          - "dpanel"
          - "saleor"
          - "saleor-dashboard"
      postgres_major:
        description: "Postgres major version for postgres-cnpg build"
        required: true
        default: "18"
        type: string
      version:
        description: "Select Docker Image Version"
        required: true
        default: "latest"
        type: string

env:
  GITHUB_IMAGE_REGISTRY: ghcr.io
  GITHUB_IMAGE_NAME: nobinkhan/docker-stack
  POSTGRES_UPSTREAM_IMAGE: cgr.dev/chainguard/postgres:latest
  SALEOR_UPSTREAM_IMAGE: ghcr.io/saleor/saleor:latest
  SALEOR_DASHBOARD_UPSTREAM_IMAGE: ghcr.io/saleor/saleor-dashboard
  DPANEL_UPSTREAM_IMAGE: docker.io/dpanel/dpanel:lite

jobs:
  Build:
    name: "Building Image"
    runs-on: "ubuntu-latest"
    permissions:
      contents: write
      packages: write
      id-token: write

    steps:
      - name: "Checkout Code"
        uses: "actions/checkout@v4"

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Log into github registry ${{ env.REGISTRY }}
        uses: docker/login-action@v3
        with:
          registry: ${{ env.GITHUB_IMAGE_REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract Image Version
        id: extract-version
        run: |
          if [ "${{ github.event.inputs.image }}" = "postgres" ]; then
            docker pull ${{ env.POSTGRES_UPSTREAM_IMAGE }}
            version=$(docker run --rm --user postgres --entrypoint postgres ${{ env.POSTGRES_UPSTREAM_IMAGE }} --version | awk '{print $NF}')
            echo "version=$version" >> $GITHUB_OUTPUT
            echo "image_short_name=postgres" >> $GITHUB_OUTPUT
          elif [ "${{ github.event.inputs.image }}" = "postgres-cnpg" ]; then
            docker build \
              --build-arg PG_MAJOR=${{ github.event.inputs.postgres_major }} \
              --platform linux/amd64 \
              -t local/postgres-cnpg:build \
              -f dockerfiles/Dockerfile.postgresql.cnpg .
            version=$(docker run --rm --entrypoint postgres local/postgres-cnpg:build --version | awk '{print $NF}')
            echo "version=$version" >> $GITHUB_OUTPUT
            echo "image_short_name=postgres-cnpg" >> $GITHUB_OUTPUT
          else
            echo "nothing to work with"
          fi

      - name: Re-Tag and push to GitHub
        id: push-to-github
        run: |
          if [ "${{ github.event.inputs.image }}" = "postgres" ]; then
            docker pull ${{ env.POSTGRES_UPSTREAM_IMAGE }}

            docker tag ${{ env.POSTGRES_UPSTREAM_IMAGE }} ${{ env.GITHUB_IMAGE_REGISTRY }}/${{ env.GITHUB_IMAGE_NAME }}/${{ steps.extract-version.outputs.image_short_name }}:${{ steps.extract-version.outputs.version }}-wolfi
            docker tag ${{ env.POSTGRES_UPSTREAM_IMAGE }} ${{ env.GITHUB_IMAGE_REGISTRY }}/${{ env.GITHUB_IMAGE_NAME }}/${{ steps.extract-version.outputs.image_short_name }}:latest
            docker tag ${{ env.POSTGRES_UPSTREAM_IMAGE }} ${{ secrets.DOCKERHUB_USERNAME }}/${{ steps.extract-version.outputs.image_short_name }}:${{ steps.extract-version.outputs.version }}-wolfi
            docker tag ${{ env.POSTGRES_UPSTREAM_IMAGE }} ${{ secrets.DOCKERHUB_USERNAME }}/${{ steps.extract-version.outputs.image_short_name }}:latest

            docker push ${{ env.GITHUB_IMAGE_REGISTRY }}/${{ env.GITHUB_IMAGE_NAME }}/${{ steps.extract-version.outputs.image_short_name }}:${{ steps.extract-version.outputs.version }}-wolfi
            docker push ${{ env.GITHUB_IMAGE_REGISTRY }}/${{ env.GITHUB_IMAGE_NAME }}/${{ steps.extract-version.outputs.image_short_name }}:latest
            docker push ${{ secrets.DOCKERHUB_USERNAME }}/${{ steps.extract-version.outputs.image_short_name }}:${{ steps.extract-version.outputs.version }}-wolfi
            docker push ${{ secrets.DOCKERHUB_USERNAME }}/${{ steps.extract-version.outputs.image_short_name }}:latest

          elif [ "${{ github.event.inputs.image }}" = "postgres-cnpg" ]; then
            docker tag local/postgres-cnpg:build ${{ env.GITHUB_IMAGE_REGISTRY }}/${{ env.GITHUB_IMAGE_NAME }}/${{ steps.extract-version.outputs.image_short_name }}:${{ steps.extract-version.outputs.version }}-wolfi-cnpg
            docker tag local/postgres-cnpg:build ${{ env.GITHUB_IMAGE_REGISTRY }}/${{ env.GITHUB_IMAGE_NAME }}/${{ steps.extract-version.outputs.image_short_name }}:latest
            docker tag local/postgres-cnpg:build ${{ secrets.DOCKERHUB_USERNAME }}/${{ steps.extract-version.outputs.image_short_name }}:${{ steps.extract-version.outputs.version }}-wolfi-cnpg
            docker tag local/postgres-cnpg:build ${{ secrets.DOCKERHUB_USERNAME }}/${{ steps.extract-version.outputs.image_short_name }}:latest

            docker push ${{ env.GITHUB_IMAGE_REGISTRY }}/${{ env.GITHUB_IMAGE_NAME }}/${{ steps.extract-version.outputs.image_short_name }}:${{ steps.extract-version.outputs.version }}-wolfi-cnpg
            docker push ${{ env.GITHUB_IMAGE_REGISTRY }}/${{ env.GITHUB_IMAGE_NAME }}/${{ steps.extract-version.outputs.image_short_name }}:latest
            docker push ${{ secrets.DOCKERHUB_USERNAME }}/${{ steps.extract-version.outputs.image_short_name }}:${{ steps.extract-version.outputs.version }}-wolfi-cnpg
            docker push ${{ secrets.DOCKERHUB_USERNAME }}/${{ steps.extract-version.outputs.image_short_name }}:latest

          elif [ "${{ github.event.inputs.image }}" = "saleor" ]; then
            docker build --build-arg BASE_IMAGE=${{ env.SALEOR_UPSTREAM_IMAGE }} --platform linux/amd64 -t ${{ env.GITHUB_IMAGE_REGISTRY }}/${{ env.GITHUB_IMAGE_NAME }}/${{ github.event.inputs.image }}:latest -t ${{ secrets.DOCKERHUB_USERNAME }}/${{ github.event.inputs.image }}:latest -f dockerfiles/Dockerfile.saleor-core --push ./dockerfiles

          elif [ "${{ github.event.inputs.image }}" = "saleor-dashboard" ]; then
            docker pull ${{ env.SALEOR_DASHBOARD_UPSTREAM_IMAGE }}

            docker tag ${{ env.SALEOR_DASHBOARD_UPSTREAM_IMAGE }} ${{ env.GITHUB_IMAGE_REGISTRY }}/${{ env.GITHUB_IMAGE_NAME }}/${{ github.event.inputs.image }}:${{ github.event.inputs.version }}
            docker tag ${{ env.SALEOR_DASHBOARD_UPSTREAM_IMAGE }} ${{ secrets.DOCKERHUB_USERNAME }}/${{ github.event.inputs.image }}:${{ github.event.inputs.version }}

            docker push ${{ env.GITHUB_IMAGE_REGISTRY }}/${{ env.GITHUB_IMAGE_NAME }}/${{ github.event.inputs.image }}:${{ github.event.inputs.version }}
            docker push ${{ secrets.DOCKERHUB_USERNAME }}/${{ github.event.inputs.image }}:${{ github.event.inputs.version }}

          elif [ "${{ github.event.inputs.image }}" = "dpanel" ]; then
            docker build --build-arg BASE_IMAGE=${{ env.DPANEL_UPSTREAM_IMAGE }} --platform linux/amd64 -t ${{ env.GITHUB_IMAGE_REGISTRY }}/${{ env.GITHUB_IMAGE_NAME }}/${{ github.event.inputs.image }}:latest -t ${{ secrets.DOCKERHUB_USERNAME }}/${{ github.event.inputs.image }}:latest -f dockerfiles/Dockerfile.dpanel --push ./dockerfiles
          else
            echo "nothing to work with"
          fi

      - name: Render Deploy Trigger With WebHook
        run: |
          if [ "${{ github.event.inputs.image }}" = "dpanel" ]; then
            curl "${{ secrets.DEPLOY_WEBHOOK_URL_DPANEL }}"
          elif [ "${{ github.event.inputs.image }}" = "saleor" ]; then
            curl "${{ secrets.SALEOR_BETA_API_DEPLOY_HOOK_RENDER }}"
          fi
